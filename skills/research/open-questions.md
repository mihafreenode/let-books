# Open Questions

- Should the portable parent skill be named `Engineering Governance` or `Traceable Engineering Governance`?
- Is `Documentation as Product` a standalone portable skill or a specialization that only matters in docs-heavy repositories?
- Should `CI/CD Governance` remain top-level or become the enforcement module of the governance template?
- What is the minimum install footprint for the governance template in a small repository?
- How much of the localization findings-corpus model is portable across non-Markdown systems?
- Which parts of the current evidence policy deserve a generic starter validator pack?
